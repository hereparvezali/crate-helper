[package]
name = "test-project"
version = "0.1.0"
edition = "2021"

[dependencies]
# Outdated crate to test inline update hint (⭡ 1.0.219) and hover version click
serde = "1.0.100"

# Outdated single-line inline table
tokio = { version = "1.0.0", features = ["full"] }

# Up-to-date crate (✓)
anyhow = "1.0.95"

# Multiline inline table orientation
sea-orm-migration = { version = "2.0.3", features = [
    "runtime-tokio-rustls",
    "sqlx-postgres"
]
}

[dev-dependencies]
tempfile = "3.2.0"

# Single-dependency table block orientation
# [dependencies.sea-orm-migration]
# version = "2.0.3"
# features = ["runtime-tokio-rustls", "sqlx-postgres"]
